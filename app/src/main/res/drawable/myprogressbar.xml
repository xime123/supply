<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:angle="0"
                android:centerColor="@color/blue_disable"
                android:centerY="0.75"
                android:endColor="@color/blue_disable"
                android:startColor="@color/blue_disable" />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:angle="0"
                    android:centerColor="@color/blue_disable"
                    android:centerY="0.75"
                    android:endColor="@color/blue_disable"
                    android:startColor="@color/blue_disable" />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:angle="0"
                    android:endColor="@color/cyan_click"
                    android:startColor="@color/cyan_click" />
            </shape>
        </clip>
    </item>
</layer-list>