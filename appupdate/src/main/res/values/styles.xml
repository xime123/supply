<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/bg_dialog_confirm</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="CommonDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item> <!--是否去除标题 -->
        <item name="android:windowFrame">@null</item> <!--是否去除边框-->
        <item name="android:windowIsFloating">true</item> <!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">true</item>  <!--是否半透明-->
        <item name="android:windowEnterAnimation">@anim/dialog_app_update_scale_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_app_update_scale_out</item>
    </style>
</resources>